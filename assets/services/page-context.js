/**
 * Returns the current category id thats set by the serwer.
 *
 * @returns {string|null}
 */
export function getCurrentCategoryId() {
    return window.currentCategoryId;
}

/**
 * Returns the current product id thats set by the server.
 *
 * @returns {string|null}
 */
export function getCurrentProductId() {
    return window.currentProductId;
}
